# Flask configuration
FLASK_APP=run.py
# set to production when deploy
FLASK_ENV=development 
FLASK_DEBUG=1
CREATE_ADMIN=true
HOST=127.0.0.1
PORT=5000

# Admin credentials (never commit these to version control!)
ADMIN_USERNAME=dev_admin
ADMIN_EMAIL=admin@localhost


